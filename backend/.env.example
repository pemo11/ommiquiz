# ⚠️ SECURITY WARNING ⚠️
# Copy this file to .env and fill in your actual values.
# NEVER commit .env files to version control!
# The .env file should be added to .gitignore.

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_FILE_ENABLED=true
LOG_FILE_PATH=/app/backend/logs/app.log
LOG_FILE_MAX_SIZE=10485760
LOG_FILE_BACKUP_COUNT=5

# Application Configuration
APP_NAME=ommiquiz-backend
APP_VERSION=1.0.32
ENVIRONMENT=development

# Supabase Configuration
# ⚠️ REQUIRED: Get these from Supabase Dashboard > Project Settings > API
# Supabase Project URL (e.g., https://xxxxxxxxxxxxx.supabase.co)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key (formerly called "anon key")
# Get this from: Supabase Dashboard > Project Settings > API > Project API keys > anon public
# ⚠️ IMPORTANT: This MUST be the same key used in the frontend as REACT_APP_SUPABASE_KEY
# This key is used to validate JWT tokens from the frontend
SUPABASE_PUB_KEY=your-supabase-anon-key-here

# Database URL (PostgreSQL connection string)
DATABASE_URL=postgresql://postgres:your-password@db.your-project-id.supabase.co:5432/postgres

# Site Configuration
SITE_URL=http://localhost:3000

# Download log storage (SQLite database path)
DOWNLOAD_LOG_DB_PATH=/app/backend/download_logs.db

# FastAPI Configuration
HOST=0.0.0.0
PORT=8000
WORKERS=1

# Flashcard Storage Configuration
# Options: local, s3
FLASHCARDS_STORAGE=local
FLASHCARDS_DIR=/app/flashcards

# S3/DigitalOcean Spaces Configuration (required if FLASHCARDS_STORAGE=s3)
S3_BUCKET=your-bucket-name
S3_ENDPOINT_URL=https://your-region.digitaloceanspaces.com
S3_PREFIX=flashcards/
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Example for production:
# ENVIRONMENT=production
# LOG_LEVEL=WARNING
# BETTERSTACK_ENABLED=true
# BETTERSTACK_SOURCE_TOKEN=bt_abc123def456...