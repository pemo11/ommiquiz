server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Remove API proxy for separate deployment
    # React will communicate directly with backend via OMMIQUIZ_APP_API_URL
}
