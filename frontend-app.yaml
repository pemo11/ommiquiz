name: ommiquiz-frontend

static_sites:
  - name: frontend
    source_dir: /frontend
    build_command: npm install && npm run build
    environment_slug: node-js
    output_dir: build
    envs:
      - key: REACT_APP_API_URL
        value: "https://YOUR-BACKEND-APP-URL.ondigitalocean.app/api"
    routes:
      - path: /